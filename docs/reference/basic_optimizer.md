# Basic Optimization Workflow

[`BasicOptimizer`][ropt.workflow.BasicOptimizer] is a low-level driver for
running a single optimization. New code should prefer the
[simple API](../usage/simple.md); `BasicOptimizer` remains for existing code.

::: ropt.workflow._basic_optimizer
    options:
        members: False
::: ropt.workflow.BasicOptimizer

