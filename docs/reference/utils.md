# Utility Functions

Optional helpers for scripts and applications that use `ropt`. Nothing in
`ropt` calls these; see [Keyboard Interrupts](../utilities/keyboard_interrupt.md)
for when the one below is worth reaching for.

::: ropt.utils
    options:
        show_root_members_full_path: false
